
# Inserts a blank line between shell prompts
add_newline = false

[line_break]
disabled = false

# Replace the "❯" symbol in the prompt with "➜"
[character]                            # The name of the module we are configuring is "character"
success_symbol = "[💡](bold green)"     # The "success_symbol" segment is being set to "➜" with the color "bold green"
error_symbol = "[❌](bold red) "

# Disable the package module, hiding it from the prompt completely
[package]
disabled = false

[cmd_duration]
min_time = 1
format = "took [$duration](bold yellow)"

[git_branch]
symbol = "🌱 "
truncation_length = 4
truncation_symbol = ""
disabled = true

[git_commit]
commit_hash_length = 4
tag_symbol = "🔖 "
disabled = true

[git_status]
conflicted = "🙅"
ahead = "🏎💨"
behind = "😰"
diverged = "😵"
untracked = "🤷‍"
stashed = "📦"
modified = "📝"
staged = '[++\($count\)](green)'
renamed = "👅"
deleted = "❌"
disabled = true

[status]
style = "bg:blue"
symbol = "🔴"
format = '[\[$symbol $common_meaning$signal_name$maybe_int\]]($style) '
map_symbol = true
disabled = false

[username]
style_user = "green bold"
style_root = "black bold"
format = "[$user]($style) "
disabled = false
show_always = true

[custom.home]
command = "echo 🏠 "  # shows output of command
when = """ test "$HOME" == "$PWD" """

# [custom.pcloud]
# command = "echo ☁️   "  # shows output of command
# when = """ test "/home/musa/pCloudDrive/*" == "$PWD" """
