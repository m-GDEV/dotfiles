#! /usr/bin/env python3

import os

os.chdir("/home/musa/Documents/Important/zMisc/AIOZ/")

#passwd = str(input("Enter decryption key: ")

os.system("ccrypt -d Files.tar.cpt") # decrypt
os.system("tar -xf Files.tar") # extract 
os.system("rm Files.tar") # rm old tar file



while True:

	done = input("Are you finised? [y/n] ")
	if done == 'y':
		break
	elif done == 'n':
		continue
	else:
		print("tf what did you enter? [INVALID]")
		quit()

os.system("tar -cf Files.tar Files")
os.system("rm Files -r")
os.system("ccrypt Files.tar")

print("Thank you for using ComBot's file encryption automation software! Be sure to leave him a review on RateYourBot.com")
